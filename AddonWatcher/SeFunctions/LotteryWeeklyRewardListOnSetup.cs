using Dalamud.Game;
using Dalamud.Plugin.Services;

namespace AddonWatcher.SeFunctions;

public sealed class LotteryWeeklyRewardListOnSetup : SeFunctionBase<OnAddonSetupDelegate>
{
    public LotteryWeeklyRewardListOnSetup(IPluginLog log, ISigScanner sigScanner)
        : base(log, sigScanner, "?? 89 ?? ?? ?? ?? 89 ?? ?? ?? ?? 89 ?? ?? ?? 57 48 83 ?? ?? 8B FA 48 8B ?? 48 ?? ?? ?? ?? ?? ?? BA")
    { }
}
